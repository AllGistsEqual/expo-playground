import {
    StyleSheet,
} from 'react-native'

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#000',
    },
    constrainer: {
        flexGrow: 1,
        alignItems: 'center',
    },
    headerPanel: {
        flex: 1,
        alignItems: 'center',
    },
})

export default styles
